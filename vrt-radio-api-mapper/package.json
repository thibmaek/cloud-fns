{
  "name": "vrt-radio-api-mapper",
  "scripts": {
    "start": "sls offline",
    "dev": "sls invoke local -f vrt_api_mapper",
    "build": "sls package -p dist",
    "deploy": "sls deploy"
  },
  "devDependencies": {
    "serverless-offline": "^8.4.0",
    "serverless-plugin-typescript": "^2.1.1"
  },
  "dependencies": {
    "got": "^12.0.1",
    "source-map-support": "^0.5.21"
  }
}
